import { Header } from '../../components/navbar'

const home = () => {
    return (
        <main className="bg-white w-screen h-screen">
            <Header />
            Enter
        </main>
    );
}

export default home